{
  "$schema": "../../modules.schema.json",
  "_meta": {
    "schemaVersion": 1,
    "verified": true
  },
  "slug": "instruo--ochd-expander",
  "name": "ochd expander",
  "manufacturer": "Instruo",
  "hp": 4,
  "tags": ["LFO", "UTILITY"],
  "description": "4HP expander for ochd. Adds CV control over ochd's master rate and provides inverted outputs for all 8 LFO channels, doubling available modulation waveforms.",
  "controls": [],
  "inputs": [
    {
      "name": "Speed CV",
      "description": "CV input for controlling ochd's master rate. Positive CV increases speed; negative CV slows or stalls LFOs.",
      "voltage": "bipolar"
    }
  ],
  "outputs": [
    {
      "name": "Inv LFO 1–8",
      "description": "Eight inverted triangle LFO outputs, one per ochd channel. Each is the phase-opposite of the corresponding ochd output.",
      "voltage": "10Vpp (±5V)"
    }
  ],
  "behaviors": [
    {
      "name": "Rate CV Control",
      "trigger": "CV → Speed CV input",
      "description": "The Speed CV input directly modulates ochd's master Rate, enabling voltage-controlled tempo. Works in addition to ochd's Rate knob and its own attenuverter."
    },
    {
      "name": "Inverted LFO Outputs",
      "trigger": "automatic",
      "description": "All 8 inverted outputs are always active and track the ochd LFOs in real-time. When ochd LFO rises, the corresponding inverted output falls."
    },
    {
      "name": "Phase Diversity",
      "trigger": "patching inverted outputs",
      "description": "Combining an ochd output with its corresponding inverted output creates bipolar modulation pairs. Running both to different destinations doubles modulation complexity."
    }
  ],
  "patchIdeas": [
    {
      "name": "Push-pull modulation",
      "patch": "LFO 3 out → VCF cutoff · Inv LFO 3 → VCA CV · filter opens as VCA closes",
      "diagram": "ochd expander:\n- ochd (LFO 3) >> VCF (Cutoff)\n- ochd expander (Inv LFO 3) >> VCA (CV) // mirrors LFO 3 for push-pull"
    },
    {
      "name": "Voltage-controlled LFO speed",
      "patch": "Sequencer CV → Speed CV · LFO rate follows sequence for rhythmically evolving modulation",
      "diagram": "ochd expander:\n- Sequencer (CV) >> ochd expander (Speed CV)\n- ochd (LFO 1) >> VCF (Cutoff)\n- ochd expander (Inv LFO 1) >> VCA (CV) // rate steps with sequence"
    },
    {
      "name": "Double the modulation sources",
      "patch": "Use all 8 ochd outs + all 8 inverted outs for 16 distinct triangle waves from a single 4+4HP pair",
      "diagram": "ochd expander:\n- ochd (LFO 2) >> VCO (FM)\n- ochd expander (Inv LFO 2) >> Filter (Res)\n- ochd (LFO 5) >> Pan (CV)\n- ochd expander (Inv LFO 5) >> Delay (Time) // 16 waves total from the pair"
    }
  ]
}
