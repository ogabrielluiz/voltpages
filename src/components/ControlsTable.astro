---
import type { Control } from '../types/module';

interface Props {
  controls: Control[];
}

const { controls } = Astro.props;
---

<div>
  <h2 class="text-xs uppercase tracking-[3px] text-subtle font-mono mb-4">Controls</h2>
  <table class="w-full border-collapse text-sm">
    <tbody>
      {controls.map((control, i) => (
        <tr class={i < controls.length - 1 ? "border-b border-border" : ""}>
          <td class="py-2.5 w-[72px] md:w-[88px] align-top">
            {control.scope && (
              <span class="control-scope-chip">{control.scope}</span>
            )}
          </td>
          <td class="py-2.5 w-[100px] md:w-[140px] font-semibold text-fg align-top text-sm md:text-[15px]">
            {control.name}
          </td>
          <td class="py-2.5 text-muted align-top">
            {control.description}
            {control.detail && (
              <div class="text-[12px] md:text-[13px] text-subtle leading-relaxed mt-0.5">{control.detail}</div>
            )}
          </td>
        </tr>
      ))}
    </tbody>
  </table>
</div>
